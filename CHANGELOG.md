# Changelog

## v1 (2026-03-04)

### Bug Fixes

- run .sh scripts through WSL on Windows (`1eb6f66`)

## v1 (2026-03-04)

### Features

- add changelog pipeline from conventional commits (`6fce895`)
- rewrite docker spellbook and fix .sh execution on Windows (`4bc0e1b`)
- rewrite docker spellbook with 12 polished commands (`01c3bac`)
- add interactive-rebase and commit-amend back to git spellbook (`d587f8b`)
- rewrite git spellbook with 16 practical commands (`e7a5d7b`)

### Bug Fixes

- changelog pipeline handles new file and pushes to main (`0b70e96`)
- revert docker version bump and trigger changelog on tag push only (`9fb78af`)
- add pause after output in find-commit and blame-line scripts (`9717790`)

### Chores

- bump docker spellbook version to 2.1.0 (`00c2512`)
- bump git spellbook version to 2.1.0 (`c84cc27`)

## v1 (2026-03-04)

### Features

- clean install modal without borders and ASCII art (`048d36b`)
- marketplace split-pane layout, registry-based fetching, scope-aware install (`e7f1c7a`)
- clear terminal before command execution and on return (`af71859`)

### Chores

- remove .glyph/ from tracking (now gitignored) (`a05def7`)

### Other

- Remove tasks/notes/scratchpad from PRD non-goals (`0c8f42c`)

## v1 (2026-03-04)

### Features

- refactor glyph into config-driven command palette (`3703cb1`)

## v0 (2026-03-04)

### Other

- Add automated binary install and release workflow (`01c3593`)

## v0.1.0 (2026-02-25)

### Features

- add command launcher and tasks (`42bdcf5`)

### Refactor

- 830c0ca refactor modules and core layout (`830c0ca`)

### Other

- Add binary installer and tag-based release workflow (`3d46684`)
- Add animated startup splash before spellbook (`a49bc0c`)
- Refresh UI theme across header, palette, tasks, and scratchpad (`20c214e`)
- Start in command palette with no default open module (`cdeb50d`)
- Add global Tab shortcut for next tab (`6df2cad`)
- Add scratchpad module with markdown preview and edit mode (`b6fa8fb`)
- Show command shortcuts and enable shortcuts in palette (`7ed8b96`)
- Refactor command handling and add global shortcuts config (`39c40cf`)
- Add project/global workspace support (`3778207`)
- refine module input handling and palette shortcuts (`5825a11`)
- refactor modules and core layout (`830c0ca`)
- code review (`96c070d`)
- 🎉 Init project (`ecb8a84`)

